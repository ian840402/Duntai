mixin card04(title,fileName,...items)
  .card04
    .content
      .col
        .field.title
          h3= title
        .field.list
          ul 
            each item in items
              li= item
      .col
        .field.img
          img(src=`./src/images/${fileName}`)
